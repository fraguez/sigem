<div class="container-fluid">
  <div class="row">
    <div class="panel-body">
      <%= form_for(payment_type) do |f| %>
        <% if payment_type.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(payment_type.errors.count, "error") %> prohibited this payment_type from being saved:</h2>

            <ul>
            <% payment_type.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group row">
          <div class="col-xs-2">
            <%= f.label :name, "Medio de Pago" %>
          </div>
          <div class="col-xs-10">
            <%= f.text_field :name, autofocus: true, placeholder: "Nombre", class: "form-control" %>
          </div>
        </div>

        <div class="form-group row">
          <div class="col-xs-2">
            <%= f.label :code, "Código" %>
          </div>
          <div class="col-xs-10">
            <%= f.number_field :code, autofocus: true, placeholder: "Código", class: "form-control" %>
          </div>
        </div>        

        <%= f.submit "Aceptar", class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>

