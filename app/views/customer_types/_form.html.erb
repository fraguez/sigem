<div class="container-fluid">
  <div class="row">
    <div class="panel-body">
      <%= form_for(customer_type) do |f| %>
        <% if customer_type.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(customer_type.errors.count, "error") %> prohibited this customer_type from being saved:</h2>

            <ul>
            <% customer_type.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group row">
          <div class="col-xs-2">
            <%= f.label :customer_type_id, "Tipo de Cliente" %>
          </div>
          <div class="col-xs-10">
            <%= f.text_field :name, autofocus: true, placeholder: "Descripción", class: "form-control" %>
          </div>
        </div>

        <%= f.submit "Aceptar", class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>