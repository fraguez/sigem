<div class="container-fluid">
  <div class="row">
    <div class="panel-body">
      <%= form_for(id_type) do |f| %>
        <% if id_type.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(id_type.errors.count, "error") %> prohibited this id_type from being saved:</h2>

            <ul>
            <% id_type.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group row">
          <div class="col-xs-2">
            <%= f.label :name, "Tipo de Identificación" %>
          </div>
          <div class="col-xs-10">
            <%= f.text_field :name, autofocus: true, placeholder: "Nombre", class: "form-control" %>
          </div>
        </div>

        <div class="form-group row">
          <div class="col-xs-2">
            <%= f.label :code, "Código" %>
          </div>
          <div class="col-xs-10">
            <%= f.number_field :code, autofocus: true, placeholder: "Código", class: "form-control" %>
          </div>
        </div>        

        <%= f.submit "Aceptar", class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>
