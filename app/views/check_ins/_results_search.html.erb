<table class="table">
  <thead>
    <tr>
      <%= render partial: "pager", locals: { keywords: @keywords, page: @page } %>
    </tr>
    <tr>
      <th>Nombre del Empleado</th>
      <th>Fecha de Entrada</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @check_ins.each do |check_in| %>
      <tr>
        <td><%= check_in.employee ? check_in.employee.name + ' ' + check_in.employee.lastname : '' %></td>
        <td><%= check_in.created_at.strftime("%d/%m/%Y %H:%M:%S") %></td>
        <td>
          <%= button_to check_in, { method: :delete, data: { confirm: '¿Está seguro?' }, class: "btn btn-danger" } do %>
            <span class="glyphicon glyphicon-trash" aria-hidden="true"> </span> Eliminar
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>